package com.apiguard3.internal;

/* loaded from: classes7.dex */
public final class onOptionsItemSelected {
    private static int valueOf = 1;
    private static int values;

    public static setContentView getSharedInstance(String str) {
        int i = values + 117;
        valueOf = i % 128;
        if ((i % 2 == 0 ? '+' : ')') != '+') {
            return APIGuard(UrlMatcher.AGState(str));
        }
        APIGuard(UrlMatcher.AGState(str));
        throw new NullPointerException();
    }

    public static setContentView valueOf(char[] cArr) {
        int i = values + 65;
        valueOf = i % 128;
        if (i % 2 != 0) {
            return APIGuard(UrlMatcher.values(cArr));
        }
        APIGuard(UrlMatcher.values(cArr));
        throw null;
    }

    private static setContentView APIGuard(byte... bArr) {
        unregisterForContextMenu values2;
        int i = values + 61;
        int i2 = i % 128;
        valueOf = i2;
        if (i % 2 == 0) {
            throw null;
        }
        if ((bArr != null ? '_' : (char) 22) == '_') {
            values = (i2 + 23) % 128;
            if (bArr.length != 0) {
                values2 = new setVerticalScrollbarThumbDrawable();
                valueOf = (values + 9) % 128;
                return new setContentView(bArr, values2);
            }
        }
        values2 = MatchConfig$Match.values();
        return new setContentView(bArr, values2);
    }
}
