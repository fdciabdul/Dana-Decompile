package androidx.transition;

import android.view.ViewGroup;

/* loaded from: classes.dex */
public class Scene {
    private ViewGroup BuiltInFictitiousFunctionClassFactory;
    private Runnable getAuthRequestContext;

    public final void getAuthRequestContext() {
        Runnable runnable;
        if (((Scene) this.BuiltInFictitiousFunctionClassFactory.getTag(R.id.lookAheadTest)) != this || (runnable = this.getAuthRequestContext) == null) {
            return;
        }
        runnable.run();
    }

    /* JADX INFO: Access modifiers changed from: package-private */
    public static void MyBillsEntityDataFactory(ViewGroup viewGroup) {
        viewGroup.setTag(R.id.lookAheadTest, null);
    }

    public static Scene getAuthRequestContext(ViewGroup viewGroup) {
        return (Scene) viewGroup.getTag(R.id.lookAheadTest);
    }
}
