package fsimpl;

import android.view.KeyEvent;
import android.view.MotionEvent;
import android.view.Window;

/* loaded from: classes.dex */
public interface H {
    void a();

    boolean a(KeyEvent keyEvent, boolean z, Window window, Window.Callback callback);

    boolean a(MotionEvent motionEvent, I i, Window window, Window.Callback callback);
}
