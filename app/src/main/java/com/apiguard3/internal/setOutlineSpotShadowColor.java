package com.apiguard3.internal;

import com.alibaba.fastjson.parser.JSONLexer;
import id.dana.cashier.view.InputCardNumberView;
import java.io.IOException;
import java.lang.reflect.Field;
import java.lang.reflect.Type;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Iterator;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

/* loaded from: classes7.dex */
public final class setOutlineSpotShadowColor implements setPivotX {
    private static int APIGuard = 0;
    private static int parseResponseHeaders = 1;
    private final setTransitionAlpha AGState;
    private final setAlpha getSharedInstance;
    private final setVerticalFadingEdgeEnabled valueOf = setVerticalFadingEdgeEnabled.values();
    private final setAccessibilityHeading values;

    public setOutlineSpotShadowColor(setAlpha setalpha, setAccessibilityHeading setaccessibilityheading, setTransitionAlpha settransitionalpha, setTranslationZ settranslationz) {
        this.getSharedInstance = setalpha;
        this.values = setaccessibilityheading;
        this.AGState = settransitionalpha;
    }

    private boolean values(Field field, boolean z) {
        APIGuard = (parseResponseHeaders + 29) % 128;
        boolean AGState2 = AGState(field, z, this.AGState);
        APIGuard = (parseResponseHeaders + 89) % 128;
        return AGState2;
    }

    private static boolean AGState(Field field, boolean z, setTransitionAlpha settransitionalpha) {
        int i = parseResponseHeaders + 101;
        APIGuard = i % 128;
        if (i % 2 != 0) {
            settransitionalpha.values(field.getType(), z);
            throw null;
        } else if (settransitionalpha.values(field.getType(), z) || settransitionalpha.AGState(field, z)) {
            parseResponseHeaders = (APIGuard + 109) % 128;
            return false;
        } else {
            return true;
        }
    }

    private List<String> valueOf(Field field) {
        setRotationX setrotationx = (setRotationX) field.getAnnotation(setRotationX.class);
        if (setrotationx == null) {
            List<String> singletonList = Collections.singletonList(this.values.APIGuard(field));
            int i = APIGuard + 13;
            parseResponseHeaders = i % 128;
            if (i % 2 != 0) {
                return singletonList;
            }
            throw new NullPointerException();
        }
        String valueOf = setrotationx.valueOf();
        String[] sharedInstance = setrotationx.getSharedInstance();
        if (sharedInstance.length != 0) {
            ArrayList arrayList = new ArrayList(sharedInstance.length + 1);
            arrayList.add(valueOf);
            int length = sharedInstance.length;
            int i2 = 0;
            while (true) {
                if (i2 >= length) {
                    return arrayList;
                }
                arrayList.add(sharedInstance[i2]);
                i2++;
                APIGuard = (parseResponseHeaders + 29) % 128;
            }
        } else {
            APIGuard = (parseResponseHeaders + 47) % 128;
            return Collections.singletonList(valueOf);
        }
    }

    @Override // com.apiguard3.internal.setPivotX
    public final <T> setScaleX<T> valueOf(setScreenReaderFocusable setscreenreaderfocusable, setScrollbarFadingEnabled<T> setscrollbarfadingenabled) {
        Class<? super T> AGState2 = setscrollbarfadingenabled.AGState();
        if ((!Object.class.isAssignableFrom(AGState2) ? (char) 2 : '\t') != 2) {
            this.getSharedInstance.APIGuard(setscrollbarfadingenabled);
            APIGuard aPIGuard = new APIGuard(getSharedInstance(setscreenreaderfocusable, setscrollbarfadingenabled, AGState2));
            APIGuard = (parseResponseHeaders + 27) % 128;
            return aPIGuard;
        }
        int i = APIGuard + 95;
        parseResponseHeaders = i % 128;
        if ((i % 2 == 0 ? 'Q' : 'K') == 'K') {
            return null;
        }
        throw null;
    }

    private AGState AGState(setScreenReaderFocusable setscreenreaderfocusable, Field field, String str, setScrollbarFadingEnabled<?> setscrollbarfadingenabled, boolean z, boolean z2) {
        setScaleX<?> setscalex;
        boolean z3;
        parseResponseHeaders = (APIGuard + 61) % 128;
        setscrollbarfadingenabled.AGState();
        setScaleY setscaley = (setScaleY) field.getAnnotation(setScaleY.class);
        if ((setscaley != null ? 'B' : '(') != '(') {
            parseResponseHeaders = (APIGuard + 103) % 128;
            setscalex = setTranslationZ.APIGuard(this.getSharedInstance, setscreenreaderfocusable, setscrollbarfadingenabled, setscaley);
        } else {
            setscalex = null;
        }
        if (!(setscalex == null)) {
            z3 = true;
        } else {
            parseResponseHeaders = (APIGuard + 9) % 128;
            z3 = false;
        }
        if (!(setscalex != null)) {
            parseResponseHeaders = (APIGuard + 65) % 128;
            setscalex = setscreenreaderfocusable.AGState(setscrollbarfadingenabled);
        }
        return new AGState(str, z, z2, field, z3, setscalex, setscreenreaderfocusable, setscrollbarfadingenabled) { // from class: com.apiguard3.internal.setOutlineSpotShadowColor.2
            private static int analyzeResponse = 0;
            private static int parseResponseHeaders = 1;
            private /* synthetic */ boolean AGState;
            private /* synthetic */ Field APIGuard;
            private /* synthetic */ setScreenReaderFocusable generateHeaders;
            private /* synthetic */ setScaleX getSharedInstance;
            private /* synthetic */ setScrollbarFadingEnabled initialize;

            {
                this.APIGuard = field;
                this.AGState = z3;
                this.getSharedInstance = setscalex;
                this.generateHeaders = setscreenreaderfocusable;
                this.initialize = setscrollbarfadingenabled;
            }

            @Override // com.apiguard3.internal.setOutlineSpotShadowColor.AGState
            final void valueOf(setLayerType setlayertype, Object obj) throws IOException, IllegalAccessException {
                setScaleX setlayoutparams;
                parseResponseHeaders = (analyzeResponse + 89) % 128;
                Object obj2 = this.APIGuard.get(obj);
                if ((this.AGState ? (char) 21 : (char) 3) == 3) {
                    setlayoutparams = new setLayoutParams(this.generateHeaders, this.getSharedInstance, this.initialize.getSharedInstance());
                } else {
                    analyzeResponse = (parseResponseHeaders + 71) % 128;
                    setlayoutparams = this.getSharedInstance;
                }
                setlayoutparams.getSharedInstance(setlayertype, obj2);
            }

            @Override // com.apiguard3.internal.setOutlineSpotShadowColor.AGState
            public final boolean getSharedInstance(Object obj) throws IOException, IllegalAccessException {
                if ((!this.valueOf ? 'D' : JSONLexer.EOI) == 'D') {
                    parseResponseHeaders = (analyzeResponse + 121) % 128;
                    return false;
                }
                if ((this.APIGuard.get(obj) != obj ? '1' : '2') != '1') {
                    analyzeResponse = (parseResponseHeaders + 13) % 128;
                    return false;
                }
                parseResponseHeaders = (analyzeResponse + 59) % 128;
                return true;
            }
        };
    }

    private Map<String, AGState> getSharedInstance(setScreenReaderFocusable setscreenreaderfocusable, setScrollbarFadingEnabled<?> setscrollbarfadingenabled, Class<?> cls) {
        setOutlineSpotShadowColor setoutlinespotshadowcolor = this;
        LinkedHashMap linkedHashMap = new LinkedHashMap();
        if ((cls.isInterface() ? (char) 20 : InputCardNumberView.DIVIDER) != 20) {
            Type sharedInstance = setscrollbarfadingenabled.getSharedInstance();
            setScrollbarFadingEnabled<?> setscrollbarfadingenabled2 = setscrollbarfadingenabled;
            Class<?> cls2 = cls;
            while (cls2 != Object.class) {
                Field[] declaredFields = cls2.getDeclaredFields();
                int length = declaredFields.length;
                boolean z = false;
                int i = 0;
                while (i < length) {
                    parseResponseHeaders = (APIGuard + 93) % 128;
                    Field field = declaredFields[i];
                    boolean z2 = true;
                    boolean values = setoutlinespotshadowcolor.values(field, true);
                    boolean values2 = setoutlinespotshadowcolor.values(field, z);
                    if (!values) {
                        if ((values2 ? 'F' : '\b') != 'F') {
                            continue;
                            i++;
                            parseResponseHeaders = (APIGuard + 125) % 128;
                            z = false;
                            setoutlinespotshadowcolor = this;
                        }
                    }
                    setoutlinespotshadowcolor.valueOf.valueOf(field);
                    Type values3 = setForceDarkAllowed.values(setscrollbarfadingenabled2.getSharedInstance(), cls2, field.getGenericType());
                    List<String> valueOf = setoutlinespotshadowcolor.valueOf(field);
                    int size = valueOf.size();
                    AGState aGState = null;
                    int i2 = 0;
                    while (true) {
                        if (i2 >= size) {
                            z = true;
                        }
                        if (z == z2) {
                            break;
                        }
                        String str = valueOf.get(i2);
                        boolean z3 = i2 != 0 ? false : values;
                        int i3 = i2;
                        AGState aGState2 = aGState;
                        int i4 = size;
                        List<String> list = valueOf;
                        Field field2 = field;
                        aGState = !(aGState2 != null) ? (AGState) linkedHashMap.put(str, AGState(setscreenreaderfocusable, field, str, setScrollbarFadingEnabled.getSharedInstance(values3), z3, values2)) : aGState2;
                        i2 = i3 + 1;
                        values = z3;
                        size = i4;
                        valueOf = list;
                        field = field2;
                        z2 = true;
                        z = false;
                    }
                    AGState aGState3 = aGState;
                    if (aGState3 != null) {
                        StringBuilder sb = new StringBuilder();
                        sb.append(sharedInstance);
                        sb.append(" declares multiple JSON fields named ");
                        sb.append(aGState3.values);
                        throw new IllegalArgumentException(sb.toString());
                    }
                    i++;
                    parseResponseHeaders = (APIGuard + 125) % 128;
                    z = false;
                    setoutlinespotshadowcolor = this;
                }
                setscrollbarfadingenabled2 = setScrollbarFadingEnabled.getSharedInstance(setForceDarkAllowed.values(setscrollbarfadingenabled2.getSharedInstance(), cls2, cls2.getGenericSuperclass()));
                cls2 = setscrollbarfadingenabled2.AGState();
                setoutlinespotshadowcolor = this;
            }
        }
        return linkedHashMap;
    }

    /* loaded from: classes7.dex */
    public static abstract class AGState {
        final boolean valueOf;
        final String values;

        abstract boolean getSharedInstance(Object obj) throws IOException, IllegalAccessException;

        abstract void valueOf(setLayerType setlayertype, Object obj) throws IOException, IllegalAccessException;

        protected AGState(String str, boolean z) {
            this.values = str;
            this.valueOf = z;
        }
    }

    /* loaded from: classes7.dex */
    public static final class APIGuard<T> extends setScaleX<T> {
        private static int valueOf = 1;
        private static int values;
        private final Map<String, AGState> AGState;

        APIGuard(Map<String, AGState> map) {
            this.AGState = map;
        }

        @Override // com.apiguard3.internal.setScaleX
        public final void getSharedInstance(setLayerType setlayertype, T t) throws IOException {
            int i = valueOf + 85;
            values = i % 128;
            if ((i % 2 != 0 ? '5' : '1') == '5') {
                throw new ArithmeticException();
            } else if (t == null) {
                setlayertype.analyzeResponse();
            } else {
                setlayertype.AGState();
                try {
                    Iterator<AGState> it = this.AGState.values().iterator();
                    while (true) {
                        if ((it.hasNext() ? '`' : (char) 15) != '`') {
                            break;
                        }
                        AGState next = it.next();
                        if (next.getSharedInstance(t)) {
                            setlayertype.APIGuard(next.values);
                            next.valueOf(setlayertype, t);
                        }
                    }
                    valueOf = (values + 77) % 128;
                    setlayertype.getSharedInstance();
                    int i2 = valueOf + 89;
                    values = i2 % 128;
                    if (i2 % 2 == 0) {
                        return;
                    }
                    throw new ArithmeticException();
                } catch (IllegalAccessException e) {
                    throw new AssertionError(e);
                }
            }
        }
    }
}
